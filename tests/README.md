# Test suites

- `backend/tests`: API, authentication, CRM, and voice-agent tests executed with Pytest.
- `frontend/tests`: Vitest and React Testing Library component tests.
- `tests/smoke.sh`: post-deployment HTTP smoke test.
