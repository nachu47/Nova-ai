import{useEffect,useState}from'react';import{Link,useSearchParams}from'react-router-dom';import{Button}from'@/components/ui/button';import{Card,CardContent,CardHeader,CardTitle}from'@/components/ui/card';import{api,errorMessage}from'@/services/api'
export function VerifyEmailPage(){const[params]=useSearchParams();const[message,setMessage]=useState('Verifying your email…');useEffect(()=>{api.get('/auth/verify-email',{params:{token:params.get('token')||''}}).then(({data})=>setMessage(data.message)).catch(err=>setMessage(errorMessage(err)))},[params]);return <div className="grid min-h-screen place-items-center p-4"><Card className="w-full max-w-md text-center"><CardHeader><CardTitle>Email verification</CardTitle></CardHeader><CardContent><p className="text-muted-foreground">{message}</p><Button asChild className="mt-5"><Link to="/login">Continue to sign in</Link></Button></CardContent></Card></div>}
