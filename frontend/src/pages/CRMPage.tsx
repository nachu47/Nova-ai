import{useState,type FormEvent}from'react';import{Building2,Plus,Search,UserRound}from'lucide-react';import{Badge}from'@/components/ui/badge';import{Button}from'@/components/ui/button';import{Card,CardContent,CardHeader,CardTitle}from'@/components/ui/card';import{Input}from'@/components/ui/input';import{Table,TableBody,TableCell,TableHead,TableHeader,TableRow}from'@/components/ui/table';import{useApi}from'@/hooks/useApi';import{api,errorMessage}from'@/services/api';import type{Contact,Paginated}from'@/types'
type Deal={id:string;name:string;stage:string;value:number;probability:number}
export function CRMPage(){const[query,setQuery]=useState('');const{data,reload}=useApi<Paginated<Contact>>(`/crm/contacts?page_size=100${query?`&search=${encodeURIComponent(query)}`:''}`);const{data:deals}=useApi<Deal[]>('/crm/deals');const[show,setShow]=useState(false);const[form,setForm]=useState({first_name:'',last_name:'',email:'',phone:'',timezone:Intl.DateTimeFormat().resolvedOptions().timeZone});const[message,setMessage]=useState('');async function submit(e:FormEvent){e.preventDefault();try{await api.post('/crm/contacts',{...form,email:form.email||null,phone:form.phone||null});setForm({...form,first_name:'',last_name:'',email:'',phone:''});setShow(false);await reload()}catch(err){setMessage(errorMessage(err))}}return <div className="space-y-6"><div className="flex items-start justify-between"><div><h1 className="page-title">CRM</h1><p className="page-subtitle">Contacts, companies, deals, notes, tasks, and a complete activity timeline.</p></div><Button onClick={()=>setShow(v=>!v)}><Plus className="mr-2 h-4 w-4"/>Contact</Button></div><div className="grid gap-4 sm:grid-cols-3"><Card><CardContent className="flex items-center gap-4 p-5"><UserRound className="h-8 w-8 text-primary"/><div><p className="text-2xl font-semibold">{data?.total??0}</p><p className="text-sm text-muted-foreground">Contacts</p></div></CardContent></Card><Card><CardContent className="flex items-center gap-4 p-5"><Building2 className="h-8 w-8 text-primary"/><div><p className="text-2xl font-semibold">{deals?.length??0}</p><p className="text-sm text-muted-foreground">Active deals</p></div></CardContent></Card><Card><CardContent className="p-5"><p className="text-sm text-muted-foreground">Pipeline value</p><p className="mt-1 text-2xl font-semibold">£{(deals??[]).reduce((n,d)=>n+Number(d.value),0).toLocaleString('en-GB')}</p></CardContent></Card></div>{show&&<Card><CardHeader><CardTitle>Add contact</CardTitle></CardHeader><CardContent><form onSubmit={submit} className="grid gap-3 md:grid-cols-2">{Object.entries(form).map(([key,value])=><label key={key} className="text-sm font-medium capitalize">{key.replace('_',' ')}<Input className="mt-1.5" value={value} onChange={e=>setForm({...form,[key]:e.target.value})} required={key==='first_name'}/></label>)}{message&&<p className="text-sm text-destructive md:col-span-2">{message}</p>}<div className="flex gap-2 md:col-span-2"><Button>Add contact</Button><Button type="button" variant="outline" onClick={()=>setShow(false)}>Cancel</Button></div></form></CardContent></Card>}<Card><CardHeader><div className="flex flex-col justify-between gap-3 sm:flex-row sm:items-center"><CardTitle>Contacts</CardTitle><div className="relative"><Search className="absolute left-3 top-2.5 h-4 w-4 text-muted-foreground"/><Input className="pl-9" placeholder="Search contacts" value={query} onChange={e=>setQuery(e.target.value)}/></div></div></CardHeader><CardContent><Table><TableHeader><TableRow><TableHead>Name</TableHead><TableHead>Email</TableHead><TableHead>Phone</TableHead><TableHead>Timezone</TableHead><TableHead>Status</TableHead></TableRow></TableHeader><TableBody>{data?.items.map(contact=><TableRow key={contact.id}><TableCell className="font-medium">{contact.first_name} {contact.last_name}</TableCell><TableCell>{contact.email||'—'}</TableCell><TableCell>{contact.phone||'—'}</TableCell><TableCell>{contact.timezone}</TableCell><TableCell>{contact.do_not_call?<Badge className="text-destructive">Do not call</Badge>:<Badge className="text-emerald-500">Callable</Badge>}</TableCell></TableRow>)}{!data?.items.length&&<TableRow><TableCell colSpan={5} className="py-10 text-center text-muted-foreground">No contacts match this search.</TableCell></TableRow>}</TableBody></Table></CardContent></Card></div>}
