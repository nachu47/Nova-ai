import{useState,type FormEvent}from'react';import{PhoneCall,Play}from'lucide-react';import{Badge}from'@/components/ui/badge';import{Button}from'@/components/ui/button';import{Card,CardContent,CardDescription,CardHeader,CardTitle}from'@/components/ui/card';import{Input}from'@/components/ui/input';import{Table,TableBody,TableCell,TableHead,TableHeader,TableRow}from'@/components/ui/table';import{useApi}from'@/hooks/useApi';import{duration,money}from'@/lib/utils';import{api,errorMessage}from'@/services/api';import type{Agent,Call,Paginated}from'@/types'
export function CallsPage(){const{data,reload,loading}=useApi<Paginated<Call>>('/calls?page_size=100');const{data:agents}=useApi<Agent[]>('/agents');const[agentId,setAgentId]=useState('');const[number,setNumber]=useState('');const[message,setMessage]=useState('');const[busy,setBusy]=useState(false);async function create(e:FormEvent){e.preventDefault();setBusy(true);setMessage('');try{await api.post('/calls/outbound',{agent_id:agentId||agents?.[0]?.id,to_number:number,context:{source:'dashboard'}});setNumber('');setMessage('Call queued successfully.');await reload()}catch(err){setMessage(errorMessage(err))}finally{setBusy(false)}}async function simulate(id:string){try{await api.post(`/calls/${id}/simulate`);setMessage('Mock call completed and summary queued.');await reload()}catch(err){setMessage(errorMessage(err))}}return <div className="space-y-6"><div><h1 className="page-title">Calls</h1><p className="page-subtitle">Start, monitor, review, and audit inbound and outbound AI calls.</p></div><Card><CardHeader><CardTitle>New outbound call</CardTitle><CardDescription>Mock mode runs locally. Twilio mode places a real call using the same endpoint.</CardDescription></CardHeader><CardContent><form onSubmit={create} className="grid gap-3 md:grid-cols-[1fr_1fr_auto]"><select className="h-10 rounded-md border bg-background px-3 text-sm" value={agentId} onChange={e=>setAgentId(e.target.value)} required><option value="">Select an agent</option>{agents?.filter(a=>a.is_active).map(a=><option key={a.id} value={a.id}>{a.name}</option>)}</select><Input value={number} onChange={e=>setNumber(e.target.value)} placeholder="E.164 number, e.g. +442079460000" required/><Button disabled={busy||!agents?.length}><PhoneCall className="mr-2 h-4 w-4"/>{busy?'Queuing…':'Start call'}</Button></form>{message&&<p className="mt-3 text-sm text-muted-foreground">{message}</p>}</CardContent></Card><Card><CardHeader><CardTitle>Call history</CardTitle></CardHeader><CardContent>{loading?<p>Loading calls…</p>:<Table><TableHeader><TableRow><TableHead>Number</TableHead><TableHead>Direction</TableHead><TableHead>Status</TableHead><TableHead>Duration</TableHead><TableHead>Revenue</TableHead><TableHead>Created</TableHead><TableHead/></TableRow></TableHeader><TableBody>{data?.items.map(call=><TableRow key={call.id}><TableCell className="font-medium">{call.direction==='outbound'?call.to_number:call.from_number}</TableCell><TableCell className="capitalize">{call.direction}</TableCell><TableCell><Badge>{call.status}</Badge></TableCell><TableCell>{duration(call.duration_seconds)}</TableCell><TableCell>{money(Number(call.revenue))}</TableCell><TableCell>{new Date(call.created_at).toLocaleString()}</TableCell><TableCell>{call.status!=='completed'&&<Button size="sm" variant="outline" onClick={()=>void simulate(call.id)}><Play className="mr-1.5 h-3.5 w-3.5"/>Simulate</Button>}</TableCell></TableRow>)}{!data?.items.length&&<TableRow><TableCell colSpan={7} className="py-10 text-center text-muted-foreground">No calls found.</TableCell></TableRow>}</TableBody></Table>}</CardContent></Card></div>}
