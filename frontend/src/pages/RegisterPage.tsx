import{useState,type FormEvent}from'react';import{Link,Navigate}from'react-router-dom';import{Button}from'@/components/ui/button';import{Card,CardContent,CardDescription,CardHeader,CardTitle}from'@/components/ui/card';import{Input}from'@/components/ui/input';import{useAuth}from'@/contexts/AuthContext';import{errorMessage}from'@/services/api'
export function RegisterPage(){const{user,register}=useAuth();const[form,setForm]=useState({organization_name:'',full_name:'',email:'',password:'',timezone:Intl.DateTimeFormat().resolvedOptions().timeZone});const[error,setError]=useState('');const[busy,setBusy]=useState(false);if(user)return <Navigate to="/" replace/>;async function submit(e:FormEvent){e.preventDefault();setBusy(true);setError('');try{await register(form)}catch(err){setError(errorMessage(err))}finally{setBusy(false)}}return <div className="grid min-h-screen place-items-center p-4"><Card className="w-full max-w-lg"><CardHeader><CardTitle className="text-2xl">Create your Nova workspace</CardTitle><CardDescription>Start with the secure local demo, then connect production providers.</CardDescription></CardHeader><CardContent><form className="grid gap-4 sm:grid-cols-2" onSubmit={submit}>{[['organization_name','Organisation'],['full_name','Full name'],['email','Email'],['password','Password'],['timezone','Timezone']].map(([key,label])=><label key={key} className={key==='timezone'?'sm:col-span-2':'block'}><span className="text-sm font-medium">{label}</span><Input className="mt-1.5" type={key==='password'?'password':key==='email'?'email':'text'} value={form[key as keyof typeof form]} onChange={e=>setForm({...form,[key]:e.target.value})} minLength={key==='password'?12:undefined} required/></label>)}{error&&<p className="sm:col-span-2 rounded-md bg-destructive/10 p-3 text-sm text-destructive">{error}</p>}<Button className="sm:col-span-2" disabled={busy}>{busy?'Creating…':'Create workspace'}</Button><Link className="sm:col-span-2 text-center text-sm text-primary hover:underline" to="/login">Back to sign in</Link></form></CardContent></Card></div>}
