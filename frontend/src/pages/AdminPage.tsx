import{useState}from'react';import{ShieldCheck}from'lucide-react';import{Badge}from'@/components/ui/badge';import{Button}from'@/components/ui/button';import{Card,CardContent,CardHeader,CardTitle}from'@/components/ui/card';import{Table,TableBody,TableCell,TableHead,TableHeader,TableRow}from'@/components/ui/table';import{useAuth}from'@/contexts/AuthContext';import{useApi}from'@/hooks/useApi';import{api}from'@/services/api';import type{User}from'@/types'
type Platform={organisations:number;users:number;calls:number;usage_revenue:number}
export function AdminPage(){const{user}=useAuth();const{data,reload}=useApi<User[]>('/admin/users');const{data:platform}=useApi<Platform>('/admin/platform');const[message,setMessage]=useState('');if(user?.role!=='owner'&&user?.role!=='admin')return <p className="text-destructive">Administrator access is required.</p>;async function toggle(target:User){await api.patch(`/admin/users/${target.id}`,{is_active:!target.is_active});setMessage(`${target.full_name} was ${target.is_active?'deactivated':'activated'}.`);await reload()}return <div className="space-y-6"><div><h1 className="page-title">Administration</h1><p className="page-subtitle">Manage users, agents, calls, API keys, usage, settings, and audit records.</p></div><div className="grid gap-4 sm:grid-cols-2 lg:grid-cols-4">{Object.entries(platform??{}).map(([key,value])=><Card key={key}><CardContent className="p-5"><ShieldCheck className="h-5 w-5 text-primary"/><p className="mt-3 text-2xl font-semibold">{typeof value==='number'?value.toLocaleString('en-GB'):value}</p><p className="text-sm capitalize text-muted-foreground">{key.replace('_',' ')}</p></CardContent></Card>)}</div>{message&&<p className="text-sm text-muted-foreground">{message}</p>}<Card><CardHeader><CardTitle>Workspace users</CardTitle></CardHeader><CardContent><Table><TableHeader><TableRow><TableHead>User</TableHead><TableHead>Role</TableHead><TableHead>Verified</TableHead><TableHead>Status</TableHead><TableHead/></TableRow></TableHeader><TableBody>{data?.map(target=><TableRow key={target.id}><TableCell><div className="font-medium">{target.full_name}</div><div className="text-xs text-muted-foreground">{target.email}</div></TableCell><TableCell className="capitalize">{target.role}</TableCell><TableCell>{target.is_verified?'Yes':'No'}</TableCell><TableCell><Badge className={target.is_active?'text-emerald-500':'text-destructive'}>{target.is_active?'Active':'Inactive'}</Badge></TableCell><TableCell><Button size="sm" variant="outline" disabled={target.id===user.id} onClick={()=>void toggle(target)}>{target.is_active?'Deactivate':'Activate'}</Button></TableCell></TableRow>)}</TableBody></Table></CardContent></Card></div>}
