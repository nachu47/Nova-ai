import {useCallback,useEffect,useState}from'react';import{api,errorMessage}from'@/services/api'
export function useApi<T>(url:string|null){const[data,setData]=useState<T|null>(null);const[loading,setLoading]=useState(true);const[error,setError]=useState('');const reload=useCallback(async()=>{if(!url){setLoading(false);return}setLoading(true);setError('');try{const response=await api.get<T>(url);setData(response.data)}catch(e){setError(errorMessage(e))}finally{setLoading(false)}},[url]);useEffect(()=>{void reload()},[reload]);return{data,loading,error,reload,setData}}
